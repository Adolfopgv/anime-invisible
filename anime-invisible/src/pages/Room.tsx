import { useEffect } from "react"
import axios from "axios"

export default function Room() {

  useEffect(() => {
    const getRoom = async () => {
      try {
        
      } catch (error) {
        
      }
    }
  })

  return (
    <div>Room</div>
  )
}
